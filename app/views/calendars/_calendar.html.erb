<div class="recipe-card">
      <div class="delete">
        <div class="inner">
          <%= link_to (image_tag 'trash.png' , class:"trash hide"), calendar_path(calendar.id), method: :delete %>
        </div>
      </div>

   <div class="card-text">
      <% @id = calendar.recipe_id %>
      <% @recipe =  Recipe.find(@id) %>
      <%= @recipe.name %>
   </div>
</div>



